#
# Host is defined as ssh_host and ssh_user
#

development ansible_connection=local

# To test ansible
staging     ansible_connection=local
production  ansible_connection=local

#staging     ansible_ssh_host=xxx ansible_ssh_user=xxx
#production  ansible_ssh_host=xxx ansible_ssh_user=xxx
