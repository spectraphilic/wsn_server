#
# These variables are common to the project, independent of
# the environment (development, staging, production, ...)
#

#python_version : "3.7"

# This is automacally found, but can be explicetely set
django_repo    : "https://github.com/spectraphilic/wsn_server.git"
