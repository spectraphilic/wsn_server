#
# Git pull
#
# Input   : django_repo, django_branch, django_root
# Output  :
# Requires:
#

- name: "Git pull"
  git:
    #repo   : "file:///{{ cwd }}"
    repo   : "{{ django_repo }}"
    dest   : "{{ django_root }}"
    version: "{{ django_branch }}"
