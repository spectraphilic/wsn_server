dist: xenial
language: python
python: 3.6

services: postgresql

install:
  - pip install ansible
  - psql -c 'create database wsn;' -U postgres
  - ansible-playbook -i ansible/hosts ansible/travis.yml

script: pytest
