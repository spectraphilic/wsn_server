{
  "name": "django-boot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run minify && npm run rename",
    "serve": "vite preview",
    "minify": "esbuild var/build/main.js --minify --outdir=var/build --allow-overwrite",
    "rename": "sh ansible/rename.sh"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.34",
    "@urql/svelte": "^2.0.2",
    "graphql": "^16.2.0",
    "sass": "^1.49.8",
    "svelte": "^3.46.1",
    "vite": "^2.7.10"
  }
}
